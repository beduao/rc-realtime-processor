# Worker + API (roda no Pi; e também no Mac na Fase 1).
# No Raspberry Pi 3B, prefira o OpenCV do sistema para NÃO compilar:
#   sudo apt install python3-opencv      (Bookworm = OpenCV 4.6, já tem YuNet/SFace)
# e então remova/comente a linha opencv-contrib-python abaixo.
opencv-contrib-python>=4.7
numpy
fastapi
uvicorn
pyyaml
pydantic>=2
